import { Container, Row, Col, Card, CardBody, CardHeader } from 'reactstrap';

const FeaturedProducts = ( ) => {
    const str = "Featured Products"
    return (
        <Container>
            {/* <SubHeader current={str}/> */}
            <Row className="row-content">
                <Col sm='4'>
                    
                </Col>
            </Row>
        </Container>
    )
}

export default FeaturedProducts;